#!/bin/sh -e

export DESTDIR="$1"

meson setup builddir \
    --prefix=/usr \
    -Dbash_completion=disabled \
    -Dzsh_completion=disabled \
    -Dman=disabled \
    -Dtests=false \
    -Dselinux=disabled \

ninja -C builddir
ninja -C builddir install
